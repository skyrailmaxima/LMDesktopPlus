# Prototype reference

The original bundled HTML prototypes are not redistributed in the installable
source archive. The maintained implementation lives under
`src/lmdesktopplus/static/` and uses the font-free Digitalvapor component
layer with installed Linux font fallbacks.
