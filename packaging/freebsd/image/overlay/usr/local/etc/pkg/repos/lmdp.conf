// LMDesktopPlus local pkg repo, built by
// packaging/freebsd/poudriere/build-repo.sh and published under
// /usr/local/lmdp-pkgrepo. The image install (poudriere image -f packages.list)
// resolves lmdesktopplus-desktop + peers from here first.
lmdp: {
  url: "file:///usr/local/lmdp-pkgrepo",
  enabled: yes,
  priority: 100
}
